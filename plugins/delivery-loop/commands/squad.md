---
name: squad
description: User-invoked Squad-style orchestrator. Same entrypoint as /build — thin lead, grill-style planning, gated specialists, uncommitted hand-off.
---

# Squad (same as /build)

`/squad` and `/build` are one entrypoint. Load the `delivery-loop` skill with the Skill tool by
exact name `delivery-loop` and follow `/build` exactly. Never write slash-prose to load a skill.

Read `docs/learnings.md` first when that file exists and **apply** the latest same-entrypoint
entry before gating. Then run the numbered flow in the skill.
