# Language packs

Each row makes one language pack discoverable. Marker patterns are matched anywhere beneath the
repository root; build-output and dependency directories are skipped.

| Marker | Stack | Pack |
|---|---|---|
| `*.slnx`, `*.sln`, `*.csproj` | `dotnet` | `dotnet` |
